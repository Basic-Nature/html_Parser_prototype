// Bootstrap v5.3.8 bundle loader (small local file that injects the official bundle)
// This keeps a local, versioned filename while letting the browser fetch the
// minified bundle from the official CDN. It also preserves the local source
// mapping comment that devtools expect.
(function(){
  try {
    var script = document.createElement('script');
    script.src = 'https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js';
    script.async = false;
    document.head.appendChild(script);
  } catch (e) {
    // no-op: best-effort loader
    console && console.warn && console.warn('Bootstrap loader failed', e);
  }
})();
//# sourceMappingURL=bootstrap.bundle.min.js.map
