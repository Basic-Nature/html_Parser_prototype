import {describe,expect,it} from 'vitest';
import {buildTrustedSubmitPayload} from '../services/trustedExecution';
describe('F2-H trusted execution projection',()=>{
 it('uses existing trusted URL and upload payload shapes',()=>{expect(buildTrustedSubmitPayload({runMode:'trusted_url',displayLabel:'x',url:'https://example.gov/results'})).toEqual({trusted_run_mode:'trusted_url',direct_urls:['https://example.gov/results']});expect(buildTrustedSubmitPayload({runMode:'manual_upload',displayLabel:'x.csv',uploadPath:'x.csv',uploadName:'x.csv'})).toEqual({trusted_run_mode:'manual_upload',file_source:'uploads',manual_upload_path:'x.csv',manual_upload_name:'x.csv'})});
 it('uses the exact W13C Workflow identifier payload without URL authority',()=>{const payload=buildTrustedSubmitPayload({runMode:'worklist',displayLabel:'Governed Workflow task',workflowItemId:'11111111-1111-1111-1111-111111111111',workflowPassId:'22222222-2222-2222-2222-222222222222',expectedRowVersion:7});expect(payload).toEqual({workflow_item_id:'11111111-1111-1111-1111-111111111111',workflow_pass_id:'22222222-2222-2222-2222-222222222222',expected_row_version:7});expect('direct_urls' in payload).toBe(false);expect('trusted_run_mode' in payload).toBe(false)});
 it('rejects malformed Workflow identifiers and row versions',()=>{expect(()=>buildTrustedSubmitPayload({runMode:'worklist',displayLabel:'x',workflowItemId:'bad',workflowPassId:'22222222-2222-2222-2222-222222222222',expectedRowVersion:7})).toThrow();expect(()=>buildTrustedSubmitPayload({runMode:'worklist',displayLabel:'x',workflowItemId:'11111111-1111-1111-1111-111111111111',workflowPassId:'22222222-2222-2222-2222-222222222222',expectedRowVersion:0})).toThrow()});
});
