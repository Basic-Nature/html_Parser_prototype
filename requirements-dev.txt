# Developer tools (optional for CI/local)
ruff==0.6.9
pre-commit==3.8.0
mypy==1.11.2
types-requests==2.32.0.20241016
python-dotenv>=1.2.1
pytest>=9.0.2 
pytest-cov>=4.0.0
pytest-mock>=3.10.0
dotenv>=0.1.0
dotenv-linter>=0.1.0
# For local development, install the full requirements-dev.txt; for production, use requirements.txt
# JS/Markdown tooling lives in package.json; install Node/npm and run `npm install` separately.
# Development-only test helpers
# Prometheus client used for local metrics testing (not required in production runtime)
prometheus-client>=0.16.0
# Google Sheets API (for local data migration)
google-auth>=2.40.0
google-auth-oauthlib>=1.2.1
google-auth-httplib2>=0.2.0
google-api-python-client>=2.168.0
gspread>=6.2.1

